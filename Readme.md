# Git Shell Svn_style_extension
---

###### On Mac OS 

```Bash
g++ -std=c++11 -o gitupdate update.cpp
```
to compile.

> - Deploy the executable file at System paths.
> - Enter the location of your git repository.
> - do ```gitupdate``` to update your repository.
